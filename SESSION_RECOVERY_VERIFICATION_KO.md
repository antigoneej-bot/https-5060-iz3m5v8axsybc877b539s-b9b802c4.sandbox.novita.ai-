# 몽이 저장 복구 수정·검증

2026-09-14 · 기존 프리뷰 수정 3

## 수정 내용

- 스테이지 결과의 꽃·씨앗·점수·정수·미션·성장 보상을 메모리에서 계산한 뒤 암호화된 저장소에 복구용 작업 기록을 먼저 확정합니다.
- Hive 기록과 공유 정원 재화를 순서대로 저장합니다. 중간에 멈추면 다음 실행 또는 재시도에서 최종 값을 다시 적용합니다. 값을 더하는 계산을 다시 실행하지 않으므로 중복 보상을 막습니다.
- 판마다 고유한 저장 번호와 완료 기록을 사용합니다. 완료 직후 오류가 보고되어도 같은 판의 재시도는 기존 결과를 반환합니다.
- 다음 단계 이동, 무한도전 기록, 결과 화면의 일기도 같은 방식으로 처리합니다.
- 저장 실패 시 결과 화면이 멈추는 대신 '다시 저장하기' 화면이 나타납니다. 같은 선택과 같은 번호로 재시도하며, 버튼 연속 입력을 차단합니다.
- 다른 정원 변경은 미완료 작업을 먼저 복구합니다. 백업은 게임 저장과 동시에 끼어들지 않도록 순서를 보장하고, 복구용 내부 기록은 백업에서 제외합니다.
- 홈 위젯 갱신과 화면 알림은 준비 중인 결과를 저장 완료로 표시하지 않도록 처리합니다.

## 확인한 결과

전체 자동 테스트 **88개 통과**. 기존 주먹·충돌·정원·백업 테스트도 포함합니다.

새 검증은 다음을 포함합니다.

1. 복구 기록 작성 전, 복구 기록 확정 후, Hive 개별 쓰기 중, Hive 확정 후, 공유 정원 쓰기 후, 완료 처리 직후 등 여섯 경계에서 강제 오류를 발생시킵니다.
2. 실제 암호화 Hive 저장소를 닫고 다시 열어 복구한 뒤 같은 요청을 여러 번 재시도합니다. 점수·꽃·씨앗·정수와 완료 결과가 한 번만 반영되는지 확인합니다.
3. 동시 두 번 제출, 단계 이동 재시도, 복구 이후 다른 정원 재화 변경 보존을 확인합니다.
4. 무한도전·일기 중복 방지, 복구 이후 일관된 백업 생성과 내부 기록 제외를 확인합니다.
5. 위젯 테스트로 단계 이동과 결과 저장 실패 화면의 재시도 버튼이 실제 작동하고 동일 번호를 사용하는지 확인합니다.

## 검증 범위와 한계

- 웹 릴리스 빌드로 프리뷰를 갱신합니다. 이전에 제공한 설치형 APK는 이번 수정으로 다시 빌드하지 않았습니다.
- 실제 휴대폰의 터치·음향·백그라운드 전환·알림·위젯은 실기기 검증을 하지 못했습니다.
- 테스트는 저장 경계에서 예외를 주입하고 Hive를 재개방합니다. 운영체제 프로세스 강제 종료나 물리적 디스크 손상을 직접 재현한 시험은 아닙니다.
- 복구 기록이 저장되기 전에 작업이 중단되면 미완료 결과는 아직 확정되지 않습니다. 화면이 살아 있으면 재시도할 수 있습니다. 복구 기록이 확정된 뒤부터 재실행 복구가 가능합니다.
- 저장소 용량 부족 등 쓰기 오류가 계속되는 동안에는 저장 성공을 표시하지 않습니다. 저장할 수 있는 상태가 된 뒤 재시도가 필요합니다.
- 결제·광고 서버 연결은 이번 수정 범위에 포함하지 않습니다.
