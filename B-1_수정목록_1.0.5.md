# 마음냥 정원 — 버그 수정 목록 (의뢰 송부용)

| 항목 | 내용 |
|------|------|
| 앱 | 마음냥 정원 (`com.mysticcat.journal`) |
| 빌드 | **1.0.5** (versionCode **6**) |
| AAB | `app-release-1.0.5.aab` |
| 작성일 | 2026-08-08 |
| 담당 | 박정은 |
| 구분 | B-1 검수 과정에서 발견한 오류 수정 |

---

## 1. 한줄 요약

내부테스트·시나리오 검수 중 확인된 **화면 표시 / 데이터 누적 / 결제·알림·온도** 관련 오류를 수정했습니다.  
본 빌드(**1.0.5+6**)는 Play 내부테스트에 반영되어 있으니, 테스터 링크로 업데이트 후 재검수해 주시면 됩니다.

---

## 2. 수정 항목

### 2.1 화면·UX

| # | 현상 | 수정 |
|---|------|------|
| 1 | **마음온도기록** — 날짜별 막대(0~100도)가 안 보임 | `Column` 안 `FractionallySizedBox` 높이 계산 오류 수정 → 막대 정상 표시 |
| 2 | **이번 주 돌아보기** — 「지도 공유하기」 시 버튼만 보이고 카드가 비어 있음 | 공유 카드 이미지 `width: infinity` → 캐시 크기 계산 예외 수정 |
| 3 | 「오늘 만난 고양이 공유」도 동일 위험 | 같은 원인 예방 수정 적용 |
| 4 | **온보딩** — 편지 저장 실패해도 다음 단계로 넘어감 | 저장 실패 시 안내 후 편지 단계에 머무름 |

### 2.2 데이터 누적·기록

| # | 현상 | 수정 |
|---|------|------|
| 5 | 편지·출석을 해도 **마음 온도가 안 오르거나 0도만 기록** | 온도 보정 로직이 출석을 “편지 보너스 완료”로 오판하던 부분 제거 + 쓰기 직렬화 |
| 6 | 어제·오늘 편지를 썼는데 **온도 기록에 하루만 0도로 남음** | 날짜 키를 `yyyy-MM-dd`로 통일, 자정 전 어제 온도 스냅샷, 편지 있는 날 하한 보정 |
| 7 | 온보딩 **첫 편지는 저장만 되고 온도 +1이 없음** | 일반 편지와 동일하게 온도 보너스 적용 |
| 8 | 설치 며칠째인데 **「0일째 함께하는 중」** | 온보딩 미완 시 0 고정 + 연속방문만 세던 문제 → **설치일 기준 함께한 일수**로 표시. 하단 점은 편지 기록일 |
| 9 | 메인 — 「**N일째** 함께하는 중」과 「다음 단계까지 출석 **M일** 남음」이 **어긋남** (예: 2일째인데 29일 남음) | 「함께한 일수」(설치일)와 「출석일」(성장)이 1~3일 벌어지면 소급 맞춤. 헤더에 `출석 N일째 · 다음 단계까지 M일` 표시. 메인 함께일수도 출석과 동기화 |
| 10 | 업데이트 후 예전 기록이 **사라진 것처럼** 보임 | `guest` → `local_user` 데이터 **1회 마이그레이션** (prefs + Hive) |
| 11 | 마이그레이션 실패해도 “완료”로 남아 재시도 불가 | Hive 복사 **성공 시에만** 완료 플래그. `special_letters` 박스도 포함 |
| 12 | Hive 암호화 마이그레이션 중단 시 **기록 손실 위험** | 평문·암호 박스 공존 시 복구 로직 추가 |
| 13 | 성장일·데일리카드·방울 날짜 포맷 혼재 | `yyyy-MM-dd` 정규화 (하루 두 번 적립 등 방지) |

### 2.3 결제·구독

| # | 현상 | 수정 |
|---|------|------|
| 14 | 구독 **만료·해지 후에도** 앱이 프리미엄으로 남을 수 있음 | 복원/기동 시 활성 구독 없으면 로컬 프리미엄 해제 |
| 15 | 해지 링크가 **월간 SKU만** 가리킴 | 현재 플랜(월/연) SKU로 이동 |
| 16 | 구매·복원 직후 고양이 돌보기/약속 쪽에 프리미엄이 **바로 안 반영** | 구매·복원 성공 시 care/promise 상태 즉시 갱신 |

### 2.4 마음 온도·약속

| # | 현상 | 수정 |
|---|------|------|
| 17 | **오늘의 약속** 체크↔해제 반복 시 온도가 무한히 오름 | 체크 `+1`, 해제 `-1`로 환수 |
| 18 | 마음온도기록 진입 시 구독자 보너스 규칙이 어긋날 수 있음 | 실제 프리미엄 여부 반영해 보정 |

### 2.5 알림

| # | 현상 | 수정 |
|---|------|------|
| 19 | 아침/저녁 알림이 **UTC 기준**으로 잡혀 한국 시간과 어긋남 | `Asia/Seoul`(KST)으로 스케줄하도록 수정 |

---

## 3. 빌드·설치

| 항목 | 값 |
|------|------|
| versionName | `1.0.5` |
| versionCode | `6` |
| 파일 | `C:\Users\User\Desktop\마음냥_AAB\app-release-1.0.5.aab` |

**Play Console** (업로드 완료 기준)

1. 내부테스트에 **1.0.5** (versionCode **6**) 반영됨  
2. 테스터 링크로 폰 업데이트 후 아래 검수

---

## 4. 권장 재검수 (짧게)

- [ ] 마음온도기록 — 막대 그래프 보이는지, 어제/오늘 날짜·온도  
- [ ] 메인 — 「N일째 함께하는 중」과 「다음 단계까지 출석 M일」이 **같은 일수 기준**인지 (예: 2일째 → 28일 남음)  
- [ ] 편지 보내기 → 마음 온도 상승 / 기록 반영  
- [ ] 이번 주 지도 공유하기 — 카드 이미지 표시  
- [ ] 오늘의 약속 — 체크 시 +1, 해제 시 온도 감소  
- [ ] (해당 시) 구독 복원·해지 후 프리미엄 상태  
- [ ] 알림 켠 경우 — 아침/저녁이 설정한 한국 시간에 가까운지  

---

## 5. 참고

- 정적 분석(`dart analyze`) 통과 기준으로 반영했습니다.  
- 계약 범위 밖(B-2 실 OAuth·서버 동기화·iOS 등)은 포함하지 않았습니다.  
- 상세 인수 절차는 `B-1_인수인계.md`를 함께 참고해 주세요.

문의·재검수 결과 공유 부탁드립니다.  
— 개발 측 / 담당 박정은
