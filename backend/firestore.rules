rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All client access goes through authenticated server routes.
    match /{document=**} { allow read, write: if false; }
  }
}
